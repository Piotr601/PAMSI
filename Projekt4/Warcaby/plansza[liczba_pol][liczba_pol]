plansza[liczba_pol][liczba_pol]

for(int y=0; y<liczba_pol; y++)
{
	for (int x=0; x< liczba_pol; x++)
	{
		if(plansza[x][y] == 1 )
		{	
				// srodek

				if(plansza[x-1][y] == 1 && plansza [x+1][y] == 1)
					wygrana;
				if(plansza[x][y-1] && plansza[x][y+1])
					wygrana;
				if(plansza[x-1][y-1] && plansza[x+1][y+1])
					wygrana;
				if(plansza[x-1][y+1] && plansza [x+1][y+1]
					wygrana;

		}
		else if (plansza[x][y] == 2)	
	}
}